NEXT_PUBLIC_API_URL=http://localhost:8000

# Clerk auth. Create a free app at https://dashboard.clerk.com and paste
# both keys from the API Keys page. Until both are set, sign-in UI shows
# "not configured" and the rest of the app (public pages) works normally.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
